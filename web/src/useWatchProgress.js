/**
 * Attaches to the `<video>` element in `videoRef` while `video` plays:
 * resumes from the saved position and reports progress to the daemon.
 */
export function useWatchProgress(videoRef, video) {}
